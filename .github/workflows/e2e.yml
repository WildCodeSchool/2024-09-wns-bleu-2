name: e2e-tests-workflow

on: push

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # 1️⃣ Checkout the repository
      - name: Check out code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Install Playwright browsers
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # 5️⃣ Create .env for app
      - name: Create .env
        run: |
          echo "JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }}" >> .env
          echo "RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}" >> .env

      # 6️⃣ Create .db.env for database
      - name: Create .db.env
        run: |
          echo "DB_HOST=${{ secrets.DB_HOST }}" >> .db.env
          echo "DB_PORT=${{ secrets.DB_PORT }}" >> .db.env
          echo "DB_USER=${{ secrets.DB_USER }}" >> .db.env
          echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> .db.env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> .db.env

      # 7️⃣ Launch initialize profile (seed DB, create user)
      - name: Initialize database and seed
        run: docker compose -f docker-compose.e2e.yml --profile initialize up --build --abort-on-container-exit

      # 8️⃣ Launch dev profile (services + E2E)
      - name: Start Docker Compose services
        run: |
          docker compose -f docker-compose.e2e.yml --profile dev up -d
          docker compose -f docker-compose.e2e.yml --profile dev ps
          # Wait for services to be ready
          echo "Waiting 15s for services to be ready..."
          sleep 15

      # 9️⃣ Run Playwright tests
      - name: Run E2E tests
        run: npx playwright test --timeout=60000

      # 🔟 Upload test artifacts for debugging
      - name: Upload Playwright reports
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: playwright-report
          path: |
            e2e/tests/.auth/user.json
            playwright-report/
            test-results/

      # 1️⃣1️⃣ Stop Docker Compose after tests
      - name: Stop Docker Compose
        run: docker compose -f docker-compose.e2e.yml down

